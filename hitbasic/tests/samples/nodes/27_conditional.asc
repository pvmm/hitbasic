Dim lang as String

If lang = "PT" Then
	print "ano"
	If lang = "PT_PT" Then
		print "ano2"
		Print "done(3)"
		if lang = "A" then
			print "A"
		End If
	End If
	Print "done(2)"
End If

Print "done(1)"
